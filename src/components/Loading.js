import React from "react";
import "../stylesheets/components/_loading.scss";

const Loading = () => {
  return (
    <section className="loading__wrapper">
      <div className="loading"></div>;
    </section>
  );
};

export default Loading;
